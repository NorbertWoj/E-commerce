import { SnapShopValidators } from './snap-shop-validators';

describe('SnapShopValidators', () => {
  it('should create an instance', () => {
    expect(new SnapShopValidators()).toBeTruthy();
  });
});
